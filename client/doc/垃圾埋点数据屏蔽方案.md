# 垃圾埋点数据屏蔽方案

1. 后端接收到接口消息后，先校验格式是否正确，必填项是否有值，没有的话直接丢弃。
2. 接收到消息后，提取请求头里的公网IP信息，记入AE库（AE库需要扩一列）
3. 记录clientID的那张表，扩一列“最后访问IP”
4. 初次写入clientID的时候同步填写、每天的定时任务要更新“最后访问IP”
5. 看板的客户端统计，增加“最后访问IP”列
6. 看板新增一个标签“IP”统计，简单汇总显示IP地址和每个IP地址的client数，按client数从多到少排序（可能非常多，所以要做分页）